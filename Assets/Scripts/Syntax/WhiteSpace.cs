using UnityEngine;

// WhiteSpace : 공백문은 컴파일, 실행시 무시되는 문장
public class WhiteSpace : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("C#");
        Debug.  Log  (  "C#"  );
        Debug
            .Log(
               "C#");
    }

}
